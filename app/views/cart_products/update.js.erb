$('#cart_index').html("<%= j( render 'index', cart_products: @cart_products, total_price: @total_price ) %>");
$('.update_info_<%= @cart_product.id %>').html("<small class='text-success'>数量を変更しました</small>");