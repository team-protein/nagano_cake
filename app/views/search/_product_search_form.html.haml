= form_with url: products_path, method: :get, local: :true do |f|
  .input-group-append.my-3
    = f.collection_select :genre_id, Genre.all, :id, :name, { selected: params[:genre_id], prompt: 'ジャンル', include_blank: '指定しない' }, class: "btn btn-outline-secondary"
    = f.search_field :word, value: params[:word], placeholder: '商品を検索', size: 25
    = f.submit "\uF002", class: 'search-btn btn btn-sm btn-outline-primary'